import { styled } from '../../styles';

export const FileUploaderContainer = styled('div', {
    padding: '1rem 0',
    h3: {
        marginBottom: '1rem',
    },
    input: {
        display: 'block',
        marginBottom: '1rem',
        padding: '0.5rem',
        border: '1px solid #ccc',
        borderRadius: '4px',
    },
});